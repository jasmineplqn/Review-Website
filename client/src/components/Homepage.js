import { styled } from "styled-components";

const Homepage = () => {
  return (
    <Container>
      hi
    </Container>
  );
};

const Container = styled.div`
    height: 110vh;
`

export default Homepage;